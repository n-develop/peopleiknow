@model System.Collections.Generic.List<PeopleIKnow.Models.Contact>
@{
    Layout = null;
}

@if (Model != null)
{
    foreach (var contact in Model)
    {
        await Html.RenderPartialAsync("_ContactTeaser", contact);
    }
}